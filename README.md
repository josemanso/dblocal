Interfaz node.js para una base de datos local postgreSQL.

Creación de un base de datos postgresql, local con user, password, localhost, nombre de la base de datos y una tabla creada.

Interfaz:
	nodejs; con express 4; 
			views: jade;
			method-override; 
